

/**
 * Simple.java
 * by convention, identifiers that represent class names
 * (which includes program names) begin with a capital letter. This means that
 * you won't get an error if your program name doesn't
 * start with a capital letter (as long as what it starts with is legal for an
 * identifier), but it's better style if we do
 */
//class simple is public, should be declared in a file named simple.java
/**
 *
 * @author Tola Lemma
 */
class Simple {
    /**
     * @param args
     */
    public static void main(String[] args) {

        int a = 1;
        System.out.println(a++);
        System.out.println(++a);
        // display 1 and 3 
    }
}
